
{
  "entities": {
    "Patron": {
      "title": "Patron",
      "type": "object",
      "description": "Represents a library patron (student, staff, or faculty).",
      "properties": {
        "id": { "type": "string" },
        "schoolId": { "type": "string", "description": "RFID or School ID" },
        "name": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["Student", "Faculty", "Staff", "Visitor"] },
        "departments": { "type": "array", "items": { "type": "string" } },
        "age": { "type": "number" },
        "isBlocked": { "type": "boolean" },
        "blockReason": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "photoUrl": { "type": "string" }
      },
      "required": ["id", "schoolId", "name", "isBlocked"]
    },
    "Visit": {
      "title": "Visit",
      "type": "object",
      "description": "A check-in event logged at the kiosk.",
      "properties": {
        "id": { "type": "string" },
        "patronId": { "type": "string" },
        "schoolId": { "type": "string" },
        "patronName": { "type": "string" },
        "patronDepartments": { "type": "array", "items": { "type": "string" } },
        "patronAge": { "type": "number" },
        "purpose": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["granted", "blocked"] }
      },
      "required": ["id", "patronId", "timestamp", "status"]
    },
    "SystemConfig": {
      "title": "System Configuration",
      "type": "object",
      "description": "Global settings for departments, purposes, and limits.",
      "properties": {
        "departments": { 
          "type": "array", 
          "items": { 
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "name": { "type": "string" },
              "code": { "type": "string" },
              "isActive": { "type": "boolean" },
              "color": { "type": "string" }
            }
          }
        },
        "purposes": { 
          "type": "array", 
          "items": { 
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "label": { "type": "string" },
              "icon": { "type": "string" }
            }
          } 
        },
        "capacityLimit": { "type": "number" },
        "dailyEngagementTarget": { "type": "number" },
        "reportFormat": { "type": "string", "enum": ["pdf", "excel"] },
        "useLetterhead": { "type": "boolean" },
        "themeImageUrl": { "type": "string" },
        "universityLogoUrl": { "type": "string" },
        "overlayOpacity": { "type": "number" },
        "welcomeTextColor": { "type": "string", "enum": ["white", "black"] },
        "timeoutSeconds": { "type": "number", "default": 3 },
        "requireAge": { "type": "boolean", "default": true },
        "defaultChartType": { "type": "string", "enum": ["pie", "bar"] },
        "reportHeaderTitle": { "type": "string" },
        "reportFooterText": { "type": "string" },
        "blockReasons": { "type": "array", "items": { "type": "string" } }
      }
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "/patrons/{patronId}": {
      "schema": "#/entities/Patron",
      "description": "Library visitor profiles."
    },
    "/visits/{visitId}": {
      "schema": "#/entities/Visit",
      "description": "Log of all check-in attempts."
    },
    "/system_config/{configId}": {
      "schema": "#/entities/SystemConfig",
      "description": "Global terminal and report settings."
    }
  }
}
