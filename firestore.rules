
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Broad read/write access for institutional records to resolve terminal sync errors.
    // In production, these should be restricted further.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
